[project]
name = "adobe-hello-mcp"
version = "0.1.0"
description = "Minimal MCP server using Adobe OAuth (hello tool)"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.115.0",
    "uvicorn[standard]==0.30.6",
    "httpx==0.27.2",
    "python-dotenv==1.0.1",
]

[tool.uv]
dev-dependencies = []
